pandas
numpy
matplotlib
seaborn
scikit-learn
joblib
streamlit
# Optional (used on the full Kaggle dataset if available; not required
# to reproduce this project's results, which use sklearn's built-in
# resample() instead of SMOTE):
# imbalanced-learn
